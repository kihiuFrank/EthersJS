{
  "name": "ethers-simple-storage",
  "version": "1.0.0",
  "description": "",
  "main": "deploy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "yarn solcjs --bin --abi --include-path node-modules/ --base-path . -o . SimpleStorage.sol"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^5.7.0",
    "fs": "^0.0.1-security",
    "fs-extra": "^10.1.0",
    "solc": "0.8.7-fixed"
  }
}
